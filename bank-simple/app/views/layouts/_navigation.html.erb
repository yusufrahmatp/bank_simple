<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Bank Simple', root_path, class: "navbar-brand", id: "logo" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to 'Home', root_path %><span class="sr-only">(current)</span></li>
        <% if logged_in? %>
          <li><%= link_to 'Add', add_path %></a></li>
          <li><%= link_to 'Withdraw', withdraw_path %></a></li>
          
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if !logged_in? %>
          <span class="glyphicon glyphicon-user"></span><li align="right" class ="signup"><%= link_to "Sign up", signup_path %></li>
        <% else %>
          <li><%= link_to 'Edit account', edit_user_path %></a></li>
          <li><%= link_to 'Log out', logout_path, method: :delete %></a></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>